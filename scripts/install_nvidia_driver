## instaliranje nvidia vlasničkog drajvera
# sudo apt install nvidia-detect
# nvidia-detect   # proverava koja graficka kartica je instalirana i predlaze verziju drajvera
# sudo apt install nvidia-driver (xxx)

## install KVANTUM (podešavanje tema qt baziranih programe)
# sudo apt install qt5-style-kvantum qt5-style-kvantum-themes
# sudo su
# echo "export QT_STYLE_OVERRIDE=kvantum" >> /etc/environment
# exit

# ako su fontovi posle instalacije drajvera u QT programima premali:
# sudo apt install qt5ct  # u njemu podesimo fontove kasnije

## opciono:
# za dalja podesavanja u XORG-u editujemo /etc/X11/xorg.conf
# ako xorg.conf ne postoji instaliramo:
# sudo apt install nvidia-xconfig
# sudo nvidia-xconfig # proverava /etc/X11/xorg.conf i kad ga ne nadje upisuje ga sa default parametrima

## posle restarta
# u KVANTUM menadžeru podesimo teme za qt-programe (pcmanfm-ft, geany, ...)
# u qt5 menadžeru podesimo fontove qt programa
